# ---------------------------------------------------------------------------
# Multi-Agent Intelligent Document Processing (IDP) Pipeline
# Python 3.11+  --  pinned for reproducible builds.
#
# backend/pyproject.toml is the canonical dependency list; this file mirrors
# it so `pip install -r` keeps working. A test asserts they never drift.
#
#   pip install -r requirements.txt
# ---------------------------------------------------------------------------

# --- Agent orchestration ---------------------------------------------------
# LangGraph 1.x: StateGraph, conditional edges, interrupt()/Command HITL.
langgraph==1.2.11
langchain-core==1.6.1
# Durable checkpointer. Required because the human-in-the-loop resume arrives
# on a separate HTTP request, so graph state must survive between processes --
# MemorySaver would not.
# Azure Cosmos DB checkpointer (CosmosDBSaverSync), used at runtime.
langchain-azure-cosmosdb==1.0.0
# SQLite checkpointer -- used only by the test suite, which must run offline.
langgraph-checkpoint-sqlite==3.1.1

# --- Azure OpenAI ----------------------------------------------------------
# AzureChatOpenAI: vision classification, enrichment, summarisation.
langchain-openai==1.6.0

# --- Observability ---------------------------------------------------------
# LangSmith tracing for every graph run and agent node.
langsmith==0.12.1

# --- Azure services (key-based authentication) -----------------------------
azure-ai-documentintelligence==1.0.2
azure-storage-blob==12.30.1
# Validated entities are written to their own Cosmos container.
azure-cosmos==4.16.4
azure-core==1.41.0

# --- API layer -------------------------------------------------------------
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32

# --- Data modelling / configuration ---------------------------------------
pydantic==2.13.5
pydantic-settings==2.15.0
python-dotenv==1.2.3

# --- Resilience ------------------------------------------------------------
# Exponential backoff + jitter for all Azure API calls.
tenacity==9.1.4

# --- Document rendering (PDF page -> image for the vision classifier) ------
pymupdf==1.28.2
pillow==12.3.0

# --- Testing ---------------------------------------------------------------
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-mock==3.15.1
